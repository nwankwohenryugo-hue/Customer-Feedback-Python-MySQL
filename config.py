# config.py
DB_CONFIG = {
    'host':     'localhost',
    'user':     'root',          # change if needed
    'password': 'your_password', # ← NEVER push real password!
    'database': 'feedback_db'
}
